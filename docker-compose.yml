version: '3.8'

services:
  frontend:
    build: .
    container_name: task-manager-frontend
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - task-management-network

networks:
  task-management-network:
    driver: bridge
